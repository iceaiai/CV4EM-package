# Recommended Python version: 3.11 or newer
#
# Install all Python dependencies with:
#     python -m pip install -r requirements.txt
#
# tkinter is used by image_converter for folder-selection dialogs.
# It is included with most Windows and macOS Python installations.
# On Debian/Ubuntu, install it separately with:
#     sudo apt install python3-tk

hyperspy>=2.4,<3.0
numpy>=1.26,<3.0
Pillow>=10.0,<14.0
matplotlib>=3.8,<4.0
scipy>=1.11,<2.0
scikit-image>=0.25,<0.27
